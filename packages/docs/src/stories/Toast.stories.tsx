import { StoryObj, Meta } from '@storybook/react'
import { Toast, ToastProps } from '@design-system-brunopp00/react'

export default {
  title: 'Form/Toast',
  component: Toast,
  args: {},
} as Meta<ToastProps>

export const Primary: StoryObj<ToastProps> = {}
